import stdlib.arow
import gpio.arow

int main() {
	Start()
	gpioMode(23, 1)
	int i = 0
	printnum(230 + gpioRead(23))
	while i < 10000 {
		i = i + 1
		printr(230 + gpioRead(23))
		wait(10000)
	}
	End()
	return 0
}
