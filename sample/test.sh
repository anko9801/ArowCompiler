#!/bin/bash

for f in *.arow ; do
	FILE="${f%.*}"
	echo "${FILE}.arow"
	../bin/dcc ${FILE}.arow
	echo "--------------------------------------------------------------------------------------------------------------------"
done
