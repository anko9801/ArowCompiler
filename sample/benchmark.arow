import stdlib.arow

int fibo(int6 x) {
	match &x {
		0 => return 0,
		1 => return 1,
		_ => return fibo(&x - 1) + fibo(&x - 2)
	}
}

int main() {
	printnum(getPID())
	int32 start = usclock()

	printnum(fibo(45))

	printnum(usclock() - start)
	return 0
}

