import gpio.arow
import stdlib.arow

int main() {
	int i = 0
	confirm()
	gpioMode(23, 0)
	gpioWrite(23, 1)
	wait(1000000)
	gpioWrite(23, 0)
	printnum(gpioRead(2) as int?)
	printnum(gpioRead(3) as int?)
	return i
}
